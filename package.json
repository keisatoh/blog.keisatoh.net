{
  "name": "@example/basics",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "start": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "test": "bun test",
    "check": "astro sync && vp check && biome check",
    "check:fix": "astro sync && vp check --fix && biome check --write",
    "lint": "astro sync && vp lint",
    "lint:fix": "astro sync && vp lint --fix",
    "format": "vp fmt . --write && biome check --write"
  },
  "dependencies": {
    "@astrojs/rss": "^4.0.15",
    "@astrojs/sitemap": "^3.7.0",
    "@tailwindcss/vite": "^4.1.18",
    "astro": "^5.16.11",
    "astro-critters": "^2.2.1",
    "satori": "^0.26.0",
    "sharp": "^0.34.5",
    "wrangler": "^4.60.0"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.3.14",
    "@tailwindcss/typography": "^0.5.19",
    "@types/bun": "1.3.11",
    "tailwindcss": "^4.1.18",
    "vite-plus": "0.3.3"
  },
  "engines": {
    "node": ">=20"
  }
}
